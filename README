Freenet(fred) for Debian/Ubuntu
-------------------------------

o How to build

First, you need to clone fred-official and contrib-official from
Freenet GitHub repos. 

Then put things as following:

.
|-- 00update-fred.sh
|-- contrib-official
|-- debian
`-- fred-official

Now you can run the build script, called 00update-fred.sh.  You might need to install needed Build-Dep packages.  Check debian/control or follow error messages.  Built packages will be put into a directory called fred-official-dist.
