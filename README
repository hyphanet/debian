Freenet(fred) for Debian/Ubuntu
-------------------------------

This repository contains files needed to build .deb package for fred. 

o How to build

First, you need to clone fred-official and contrib-official from
Freenet GitHub repos. 

Then put things in a directory as following:

.
|-- 00update-fred.sh
|-- contrib-official
|-- debian
`-- fred-official

Now you can run the build script, 00update-fred.sh.  You might need to install needed Build-Dep packages.  Check the Build-Depends line in debian/control or follow error messages.  Built packages will be put into a directory called fred-official-dist.
